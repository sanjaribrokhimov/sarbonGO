# Документация

- **Swagger (OpenAPI)** — исходники лежат в `internal/docs/swagger/` и встраиваются в приложение (embed).  
  Документация доступна по адресу: **http://localhost:8080/swagger/index.html** при запущенном сервере.
