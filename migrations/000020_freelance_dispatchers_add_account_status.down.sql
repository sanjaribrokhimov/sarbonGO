-- Optional: remove columns (only if you want to reverse; may break app).
-- ALTER TABLE freelance_dispatchers DROP COLUMN IF EXISTS account_status, DROP COLUMN IF EXISTS photo_path;
-- ALTER TABLE deleted_freelance_dispatchers DROP COLUMN IF EXISTS account_status, DROP COLUMN IF EXISTS photo_path;
SELECT 1;
